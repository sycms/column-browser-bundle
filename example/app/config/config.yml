framework:
    secret: secret
    session: ~
    router:
        resource: %kernel.root_dir%/config/routing.yml
    templating:
        engines: ['twig']

cmf_resource:
    repositories:
        default:
            type: filesystem
            base_dir: /home/daniel/documents


twig:
    debug: true
    strict_variables: true


