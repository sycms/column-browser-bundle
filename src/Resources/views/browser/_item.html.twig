<a class="list-group-item clearfix{{ resource.path == selectedPath ? ' active' : '' }}" href="{{ path(route, { path: resource.path }) }}">
    <span class="glyphicon glyphicon-{{ resource.hasChildren ? 'folder-close' : 'file' }}"></span>
    {{ resource.name }}
</a>
